
This is legacy code.
It works, but will be replaced at some point by a newer version with 
 - triqs arrays instead of blitz++ for progressive removal of those and clarity between view, not view
 - proto operations if needed.

So don't use this class directly in C++ code.


